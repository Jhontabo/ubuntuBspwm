##########################
# wm independent hotkeys #
##########################

# terminal emulator
alt + Return
	/usr/bin/kitty

# program launcher
alt + d
	rofi -show drun -show-icons

# make sxhkd reload its configuration files:
alt + Escape
	pkill -USR1 -x sxhkd

#################
# bspwm hotkeys #
#################

# restart bspwm
alt + shift + e
	bspc wm -r

# reload sxhkd config
alt + shift + r
	pkill -USR1 -x sxhkd

# choose wallpaper
super + w
	kitty --title "Wallpaper Picker" sh -lc '~/.local/bin/wall'

# pause mode (disable keybindings)
alt + shift + p
	xdotool key --clearmodifiers Pause

# close window
alt + q
	bspc node -c

# toggle floating
alt + shift + space
	bspc node -t floating

# toggle tiling
alt + t
	bspc node -t tiled

# toggle fullscreen
alt + f
	bspc node -t fullscreen

# toggle minimized
alt + m
	bspc node -g hidden

# cycle focus (tiling -> floating -> fullscreen)
alt + space
	bspc node -t {fullscreen,tiled,floating}

# toggle tiling direction
alt + v
	bspc node -t pseudo_tiled; bspc node -t tiled

# focus vim-like
alt + {h,j,k,l}
	bspc node -f {west,south,north,east}

# move vim-like
ctrl + shift + {h,j,k,l}
	bspc node -s {west,south,north,east}

###############
# state/flags #
###############

# set the window state
alt + {shift + t,s,f}
	bspc node -t {pseudo_tiled,floating,fullscreen}

# set the node flags
alt + ctrl + {m,x,y,z}
	bspc node -g {marked,locked,sticky,private}

##############
# focus/swap #
##############

# focus the node in the given direction
alt + {_,shift + }{Left,Down,Up,Right}
	bspc node -{f,s} {west,south,north,east}

# focus the node for the given path jump
alt + {p,b,comma,period}
	bspc node -f @{parent,brother,first,second}

# focus the next/previous window in the current desktop
alt + {_,shift + }c
	bspc node -f {next,prev}.local.!hidden.window

# focus the next/previous desktop in the current monitor
alt + bracket{left,right}
	bspc desktop -f {prev,next}.local

# focus the last node/desktop
alt + {grave,Tab}
	bspc {node,desktop} -f last

# focus the older or newer node in the focus history
alt + {o,i}
	bspc wm -h off; \
	bspc node {older,newer} -f; \
	bspc wm -h on

# focus or send to the given desktop
alt + {_,shift + }{1-9,0}
	bspc {desktop -f,node -d} '^{1-9,10}'

# focus or send to the given desktop (alt + number)
alt + {1-9,0}
	bspc desktop -f '^{1-10}'

alt + shift + {1-9,0}
	bspc node -d '^{1-10}'

# move workspace to monitor
alt + shift + a
	bspc monitor -f next -s prev

alt + shift + f
	bspc monitor -f next -s next

alt + shift + d
	bspc monitor -f prev -s prev

alt + shift + s
	bspc monitor -f prev -s next

#############
# preselect #
#############

# preselect the direction
alt + ctrl + alt + {Left,Down,Up,Right}
	bspc node -p {west,south,north,east}

# preselect the ratio
alt + ctrl + {1-9}
	bspc node -o 0.{1-9}

# cancel the preselection for the focused node
alt + ctrl + space
	bspc node -p cancel

# cancel the preselection for the focused desktop
alt + ctrl + alt + space
	bspc query -N -d | xargs -I id -n 1 bspc node id -p cancel

###############
# move/resize #
###############

# resize window (vim-like)
alt + shift + {h,j,k,l}
	~/.config/bspwm/scripts/bspwm_resize {west,south,north,east}

# expand a window by moving one of its side outward
#alt + alt + {h,j,k,l}
#	bspc node -z {left -20 0,bottom 0 20,top 0 -20,right 20 0}

# contract a window by moving one of its side inward
#alt + alt + shift + {h,j,k,l}
#	bspc node -z {right -20 0,top 0 20,bottom 0 -20,left 20 0}

# move a floating window
alt + ctrl + {Left,Down,Up,Right}
	bspc node -v {-20 0,0 20,0 -20,20 0}

########################
# Custom Launchers App #
########################

# file explorer
alt + e
	~/.local/bin/file-manager-smart

# browser
alt + b
	xdg-open https://www.mozilla.org

# BurpSuite
alt + shift + ctrl + b
	burpsuite

###############
# ScreenShots #
###############

@Print
	sh -c 'if command -v flameshot >/dev/null 2>&1; then flameshot gui; else screenshot select; fi'

@Print + ctrl
	sh -c 'if command -v flameshot >/dev/null 2>&1; then flameshot full -c; else screenshot; fi'

@Print + alt
	sh -c 'if command -v flameshot >/dev/null 2>&1; then flameshot screen -c; else screenshot window; fi'

###############
# Volume (F1-F3) #
###############

F1
	sh -c 'if command -v wpctl >/dev/null 2>&1; then wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle; else amixer set "${POLYBAR_MIXER:-Master}" toggle >/dev/null 2>&1 || amixer set Master toggle >/dev/null 2>&1; fi'

F2
	sh -c 'if command -v wpctl >/dev/null 2>&1; then wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-; else amixer set "${POLYBAR_MIXER:-Master}" 5%- unmute >/dev/null 2>&1 || amixer set Master 5%- unmute >/dev/null 2>&1; fi'

F3
	sh -c 'if command -v wpctl >/dev/null 2>&1; then wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+; else amixer set "${POLYBAR_MIXER:-Master}" 5%+ unmute >/dev/null 2>&1 || amixer set Master 5%+ unmute >/dev/null 2>&1; fi'

###############
# Spotify #
###############

XF86AudioNext
	sh -c 'command -v playerctl >/dev/null 2>&1 && playerctl -p spotify next || true'

XF86AudioPrev
	sh -c 'command -v playerctl >/dev/null 2>&1 && playerctl -p spotify previous || true'

XF86AudioPlay
	sh -c 'command -v playerctl >/dev/null 2>&1 && playerctl -p spotify play-pause || true'

XF86AudioStop
	sh -c 'command -v playerctl >/dev/null 2>&1 && playerctl -p spotify pause || true'
